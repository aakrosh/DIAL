# Makefile.in generated by automake 1.10.2 from Makefile.am.
# src/Makefile.  Generated from Makefile.in by configure.

# Copyright (C) 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002,
# 2003, 2004, 2005, 2006, 2007, 2008  Free Software Foundation, Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.



# --enable-profile
# --enable-debug
# --enable-test



pkgdatadir = $(datadir)/dial
pkglibdir = $(libdir)/dial
pkgincludedir = $(includedir)/dial
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = x86_64-unknown-linux-gnu
host_triplet = x86_64-unknown-linux-gnu
bin_PROGRAMS = assemble$(EXEEXT) assemble1$(EXEEXT) \
	assemble_illumina$(EXEEXT) build_sff_index$(EXEEXT) \
	cull_components$(EXEEXT) filter$(EXEEXT) \
	filter_clusters$(EXEEXT) fish_clusters$(EXEEXT) \
	make_template$(EXEEXT) masker$(EXEEXT) \
	remove_clusters$(EXEEXT) update$(EXEEXT) \
	update_clusters$(EXEEXT)
subdir = src
DIST_COMMON = $(srcdir)/Makefile.am $(srcdir)/Makefile.in \
	$(srcdir)/config.h.in
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
mkinstalldirs = $(install_sh) -d
CONFIG_HEADER = config.h
CONFIG_CLEAN_FILES =
am__installdirs = "$(DESTDIR)$(bindir)" "$(DESTDIR)$(bindir)"
binPROGRAMS_INSTALL = $(INSTALL_PROGRAM)
PROGRAMS = $(bin_PROGRAMS)
am__objects_1 = utilities.$(OBJEXT)
am__objects_2 = common.$(OBJEXT)
am__objects_3 = slinklist.$(OBJEXT)
am__objects_4 = hashtable.$(OBJEXT)
am__objects_5 = $(am__objects_1) $(am__objects_2) $(am__objects_3) \
	$(am__objects_4)
am__objects_6 = sequences.$(OBJEXT)
am__objects_7 = $(am__objects_5) $(am__objects_6)
am__objects_8 = sff.$(OBJEXT)
am__objects_9 = contig.$(OBJEXT)
am_assemble_OBJECTS = $(am__objects_7) $(am__objects_8) \
	$(am__objects_9) assemble.$(OBJEXT)
assemble_OBJECTS = $(am_assemble_OBJECTS)
assemble_LDADD = $(LDADD)
am__objects_10 = sff.$(OBJEXT) sff_index.$(OBJEXT)
am_assemble1_OBJECTS = $(am__objects_7) $(am__objects_10) \
	$(am__objects_9) assemble1.$(OBJEXT)
assemble1_OBJECTS = $(am_assemble1_OBJECTS)
assemble1_LDADD = $(LDADD)
am_assemble_illumina_OBJECTS = $(am__objects_7) \
	assemble_illumina.$(OBJEXT)
assemble_illumina_OBJECTS = $(am_assemble_illumina_OBJECTS)
assemble_illumina_LDADD = $(LDADD)
am_build_sff_index_OBJECTS = $(am__objects_5) $(am__objects_10) \
	build_sff_index.$(OBJEXT)
build_sff_index_OBJECTS = $(am_build_sff_index_OBJECTS)
build_sff_index_LDADD = $(LDADD)
am__objects_11 = graph.$(OBJEXT)
am__objects_12 = cluster.$(OBJEXT)
am_cull_components_OBJECTS = $(am__objects_7) $(am__objects_11) \
	$(am__objects_12) cull_components.$(OBJEXT)
cull_components_OBJECTS = $(am_cull_components_OBJECTS)
cull_components_LDADD = $(LDADD)
am_filter_OBJECTS = $(am__objects_1) filter.$(OBJEXT)
filter_OBJECTS = $(am_filter_OBJECTS)
filter_LDADD = $(LDADD)
am_filter_clusters_OBJECTS = $(am__objects_5) $(am__objects_12) \
	filter_clusters.$(OBJEXT)
filter_clusters_OBJECTS = $(am_filter_clusters_OBJECTS)
filter_clusters_LDADD = $(LDADD)
am_fish_clusters_OBJECTS = $(am__objects_7) $(am__objects_11) \
	$(am__objects_12) fish_clusters.$(OBJEXT)
fish_clusters_OBJECTS = $(am_fish_clusters_OBJECTS)
fish_clusters_LDADD = $(LDADD)
am_make_template_OBJECTS = $(am__objects_1) $(am__objects_2) \
	$(am__objects_6) make_template.$(OBJEXT)
make_template_OBJECTS = $(am_make_template_OBJECTS)
make_template_LDADD = $(LDADD)
am__objects_13 = splayhash.$(OBJEXT)
am_masker_OBJECTS = $(am__objects_1) $(am__objects_2) \
	$(am__objects_13) $(am__objects_6) masker.$(OBJEXT)
masker_OBJECTS = $(am_masker_OBJECTS)
masker_DEPENDENCIES =
am__objects_14 = maf.$(OBJEXT) mfblocks.$(OBJEXT)
am_remove_clusters_OBJECTS = $(am__objects_1) $(am__objects_12) \
	$(am__objects_3) $(am__objects_14) remove_clusters.$(OBJEXT)
remove_clusters_OBJECTS = $(am_remove_clusters_OBJECTS)
remove_clusters_LDADD = $(LDADD)
am_update_OBJECTS = $(am__objects_7) update.$(OBJEXT)
update_OBJECTS = $(am_update_OBJECTS)
update_LDADD = $(LDADD)
am_update_clusters_OBJECTS = $(am__objects_7) $(am__objects_14) \
	$(am__objects_12) update_clusters.$(OBJEXT)
update_clusters_OBJECTS = $(am_update_clusters_OBJECTS)
update_clusters_DEPENDENCIES =
binSCRIPT_INSTALL = $(INSTALL_SCRIPT)
SCRIPTS = $(bin_SCRIPTS)
DEFAULT_INCLUDES = -I.
depcomp = $(SHELL) $(top_srcdir)/depcomp
am__depfiles_maybe = depfiles
COMPILE = $(CC) $(DEFS) $(DEFAULT_INCLUDES) $(INCLUDES) $(AM_CPPFLAGS) \
	$(CPPFLAGS) $(AM_CFLAGS) $(CFLAGS)
CCLD = $(CC)
LINK = $(CCLD) $(AM_CFLAGS) $(CFLAGS) $(AM_LDFLAGS) $(LDFLAGS) -o $@
SOURCES = $(assemble_SOURCES) $(assemble1_SOURCES) \
	$(assemble_illumina_SOURCES) $(build_sff_index_SOURCES) \
	$(cull_components_SOURCES) $(filter_SOURCES) \
	$(filter_clusters_SOURCES) $(fish_clusters_SOURCES) \
	$(make_template_SOURCES) $(masker_SOURCES) \
	$(remove_clusters_SOURCES) $(update_SOURCES) \
	$(update_clusters_SOURCES)
DIST_SOURCES = $(assemble_SOURCES) $(assemble1_SOURCES) \
	$(assemble_illumina_SOURCES) $(build_sff_index_SOURCES) \
	$(cull_components_SOURCES) $(filter_SOURCES) \
	$(filter_clusters_SOURCES) $(fish_clusters_SOURCES) \
	$(make_template_SOURCES) $(masker_SOURCES) \
	$(remove_clusters_SOURCES) $(update_SOURCES) \
	$(update_clusters_SOURCES)
ETAGS = etags
CTAGS = ctags
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
ACLOCAL = ${SHELL} /afs/bx.psu.edu/home/ratan/projects/DIAL/missing --run aclocal-1.10
AMTAR = ${SHELL} /afs/bx.psu.edu/home/ratan/projects/DIAL/missing --run tar
ARCH = -minline-all-stringops -momit-leaf-frame-pointer
AUTOCONF = ${SHELL} /afs/bx.psu.edu/home/ratan/projects/DIAL/missing --run autoconf
AUTOHEADER = ${SHELL} /afs/bx.psu.edu/home/ratan/projects/DIAL/missing --run autoheader
AUTOMAKE = ${SHELL} /afs/bx.psu.edu/home/ratan/projects/DIAL/missing --run automake-1.10
AWK = gawk
CC = gcc
CCDEPMODE = depmode=gcc3
CFLAGS = -g -O2
CPP = gcc -E
CPPFLAGS = 
CYGPATH_W = echo
DEFS = -DHAVE_CONFIG_H
DEPDIR = .deps
ECHO_C = 
ECHO_N = -n
ECHO_T = 
EGREP = /bin/grep -E
EXEEXT = 
GREP = /bin/grep
INSTALL = /usr/bin/install -c
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_PROGRAM = ${INSTALL}
INSTALL_SCRIPT = ${INSTALL}
INSTALL_STRIP_PROGRAM = $(install_sh) -c -s
LDFLAGS = 
LIBOBJS = 
LIBS = 
LTLIBOBJS = 
MAKEINFO = ${SHELL} /afs/bx.psu.edu/home/ratan/projects/DIAL/missing --run makeinfo
MKDIR_P = /bin/mkdir -p
OBJEXT = o
PACKAGE = dial
PACKAGE_BUGREPORT = ratan@bx.psu.edu
PACKAGE_NAME = DIAL
PACKAGE_STRING = DIAL 1.0
PACKAGE_TARNAME = dial
PACKAGE_VERSION = 1.0
PATH_SEPARATOR = :
SET_MAKE = 
SHELL = /bin/sh
STRIP = 
VERSION = 1.0
abs_builddir = /afs/bx.psu.edu/home/ratan/projects/DIAL/src
abs_srcdir = /afs/bx.psu.edu/home/ratan/projects/DIAL/src
abs_top_builddir = /afs/bx.psu.edu/home/ratan/projects/DIAL
abs_top_srcdir = /afs/bx.psu.edu/home/ratan/projects/DIAL
ac_ct_CC = gcc
am__include = include
am__leading_dot = .
am__quote = 
am__tar = ${AMTAR} chof - "$$tardir"
am__untar = ${AMTAR} xf -
bindir = ${exec_prefix}/bin
build = x86_64-unknown-linux-gnu
build_alias = 
build_cpu = x86_64
build_os = linux-gnu
build_vendor = unknown
builddir = .
datadir = ${datarootdir}
datarootdir = ${prefix}/share
docdir = ${datarootdir}/doc/${PACKAGE_TARNAME}
dvidir = ${docdir}
exec_prefix = ${prefix}
host = x86_64-unknown-linux-gnu
host_alias = 
host_cpu = x86_64
host_os = linux-gnu
host_vendor = unknown
htmldir = ${docdir}
includedir = ${prefix}/include
infodir = ${datarootdir}/info
install_sh = $(SHELL) /afs/bx.psu.edu/home/ratan/projects/DIAL/install-sh
libdir = ${exec_prefix}/lib
libexecdir = ${exec_prefix}/libexec
localedir = ${datarootdir}/locale
localstatedir = ${prefix}/var
mandir = ${datarootdir}/man
mkdir_p = /bin/mkdir -p
oldincludedir = /usr/include
pdfdir = ${docdir}
prefix = /home/ratan/projects/DIAL
program_transform_name = s,x,x,
psdir = ${docdir}
sbindir = ${exec_prefix}/sbin
sharedstatedir = ${prefix}/com
srcdir = .
sysconfdir = ${prefix}/etc
target_alias = 
top_build_prefix = ../
top_builddir = ..
top_srcdir = ..
OPTIMIZATIONS = -DNDEBUG -O2 -funroll-all-loops $(ARCH)
#OPTIMIZATIONS = -ggdb
#OPTIMIZATIONS = -ggdb -pg -DNDEBUG -O2 -funroll-all-loops $(ARCH)
#OPTIMIZATIONS = -ggdb -DDEBUG
AM_CFLAGS = -W -Wformat -Wimplicit -Wreturn-type -Wall -Werror \
            -Wunused-variable -Wunused-parameter -Wreturn-type -Wswitch \
            -Wcast-align -Winline -Wnested-externs -Wextra $(OPTIMIZATIONS) \
            -std=c99


################################################################################
CLUSTER_SRC = cluster.c cluster.h 
COMMON_SRC = common.c common.h
CONTIG_SRC = contig.c contig.h
GRAPH_SRC = graph.c graph.h
HASHTABLE_SRC = hashtable.c hashtable.h
MAF_SRC = maf.c maf.h mfblocks.c mfblocks.h
SEQUENCES_SRC = sequences.c sequences.h
SFF_SRC = sff.c sff.h
SFF_INDEX_SRC = sff.c sff.h sff_index.c sff_index.h
SLINKLIST_SRC = slinklist.c slinklist.h
SPLAYHASH_SRC = splayhash.c splayhash.h
UTILITIES_SRC = utilities.c utilities.h
LIB_NOSEQ_SRC = $(UTILITIES_SRC) $(COMMON_SRC) $(SLINKLIST_SRC) $(HASHTABLE_SRC)
LIB_SRC = $(LIB_NOSEQ_SRC) $(SEQUENCES_SRC)

################################################################################
assemble_SOURCES = $(LIB_SRC) $(SFF_SRC) $(CONTIG_SRC) assemble.c
assemble1_SOURCES = $(LIB_SRC) $(SFF_INDEX_SRC) $(CONTIG_SRC) assemble1.c
assemble_illumina_SOURCES = $(LIB_SRC) assemble_illumina.c
build_sff_index_SOURCES = $(LIB_NOSEQ_SRC) $(SFF_INDEX_SRC) build_sff_index.c
cull_components_SOURCES = $(LIB_SRC) $(GRAPH_SRC) $(CLUSTER_SRC) \
                          cull_components.c

filter_SOURCES = $(UTILITIES_SRC) filter.c
filter_clusters_SOURCES = $(LIB_NOSEQ_SRC) $(CLUSTER_SRC) filter_clusters.c
fish_clusters_SOURCES = $(LIB_SRC) $(GRAPH_SRC) $(CLUSTER_SRC) fish_clusters.c
make_template_SOURCES = $(UTILITIES_SRC) $(COMMON_SRC) $(SEQUENCES_SRC) \
                        make_template.c

masker_SOURCES = $(UTILITIES_SRC) $(COMMON_SRC) $(SPLAYHASH_SRC) \
                 $(SEQUENCES_SRC) masker.c

masker_LDADD = -lm
remove_clusters_SOURCES = $(UTILITIES_SRC) $(CLUSTER_SRC) $(SLINKLIST_SRC) \
                          $(MAF_SRC) remove_clusters.c

update_SOURCES = $(LIB_SRC) update.c
update_clusters_SOURCES = $(LIB_SRC) $(MAF_SRC) $(CLUSTER_SRC) update_clusters.c
update_clusters_LDADD = -lm

################################################################################
bin_SCRIPTS = DIAL convertFastqFasta partition remove_clones update_status
all: config.h
	$(MAKE) $(AM_MAKEFLAGS) all-am

.SUFFIXES:
.SUFFIXES: .c .o .obj
$(srcdir)/Makefile.in:  $(srcdir)/Makefile.am  $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      ( cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh ) \
	        && { if test -f $@; then exit 0; else break; fi; }; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --gnu  src/Makefile'; \
	cd $(top_srcdir) && \
	  $(AUTOMAKE) --gnu  src/Makefile
.PRECIOUS: Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe);; \
	esac;

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure:  $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4):  $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

config.h: stamp-h1
	@if test ! -f $@; then \
	  rm -f stamp-h1; \
	  $(MAKE) $(AM_MAKEFLAGS) stamp-h1; \
	else :; fi

stamp-h1: $(srcdir)/config.h.in $(top_builddir)/config.status
	@rm -f stamp-h1
	cd $(top_builddir) && $(SHELL) ./config.status src/config.h
$(srcdir)/config.h.in:  $(am__configure_deps) 
	cd $(top_srcdir) && $(AUTOHEADER)
	rm -f stamp-h1
	touch $@

distclean-hdr:
	-rm -f config.h stamp-h1
install-binPROGRAMS: $(bin_PROGRAMS)
	@$(NORMAL_INSTALL)
	test -z "$(bindir)" || $(MKDIR_P) "$(DESTDIR)$(bindir)"
	@list='$(bin_PROGRAMS)'; for p in $$list; do \
	  p1=`echo $$p|sed 's/$(EXEEXT)$$//'`; \
	  if test -f $$p \
	  ; then \
	    f=`echo "$$p1" | sed 's,^.*/,,;$(transform);s/$$/$(EXEEXT)/'`; \
	   echo " $(INSTALL_PROGRAM_ENV) $(binPROGRAMS_INSTALL) '$$p' '$(DESTDIR)$(bindir)/$$f'"; \
	   $(INSTALL_PROGRAM_ENV) $(binPROGRAMS_INSTALL) "$$p" "$(DESTDIR)$(bindir)/$$f" || exit 1; \
	  else :; fi; \
	done

uninstall-binPROGRAMS:
	@$(NORMAL_UNINSTALL)
	@list='$(bin_PROGRAMS)'; for p in $$list; do \
	  f=`echo "$$p" | sed 's,^.*/,,;s/$(EXEEXT)$$//;$(transform);s/$$/$(EXEEXT)/'`; \
	  echo " rm -f '$(DESTDIR)$(bindir)/$$f'"; \
	  rm -f "$(DESTDIR)$(bindir)/$$f"; \
	done

clean-binPROGRAMS:
	-test -z "$(bin_PROGRAMS)" || rm -f $(bin_PROGRAMS)
assemble$(EXEEXT): $(assemble_OBJECTS) $(assemble_DEPENDENCIES) 
	@rm -f assemble$(EXEEXT)
	$(LINK) $(assemble_OBJECTS) $(assemble_LDADD) $(LIBS)
assemble1$(EXEEXT): $(assemble1_OBJECTS) $(assemble1_DEPENDENCIES) 
	@rm -f assemble1$(EXEEXT)
	$(LINK) $(assemble1_OBJECTS) $(assemble1_LDADD) $(LIBS)
assemble_illumina$(EXEEXT): $(assemble_illumina_OBJECTS) $(assemble_illumina_DEPENDENCIES) 
	@rm -f assemble_illumina$(EXEEXT)
	$(LINK) $(assemble_illumina_OBJECTS) $(assemble_illumina_LDADD) $(LIBS)
build_sff_index$(EXEEXT): $(build_sff_index_OBJECTS) $(build_sff_index_DEPENDENCIES) 
	@rm -f build_sff_index$(EXEEXT)
	$(LINK) $(build_sff_index_OBJECTS) $(build_sff_index_LDADD) $(LIBS)
cull_components$(EXEEXT): $(cull_components_OBJECTS) $(cull_components_DEPENDENCIES) 
	@rm -f cull_components$(EXEEXT)
	$(LINK) $(cull_components_OBJECTS) $(cull_components_LDADD) $(LIBS)
filter$(EXEEXT): $(filter_OBJECTS) $(filter_DEPENDENCIES) 
	@rm -f filter$(EXEEXT)
	$(LINK) $(filter_OBJECTS) $(filter_LDADD) $(LIBS)
filter_clusters$(EXEEXT): $(filter_clusters_OBJECTS) $(filter_clusters_DEPENDENCIES) 
	@rm -f filter_clusters$(EXEEXT)
	$(LINK) $(filter_clusters_OBJECTS) $(filter_clusters_LDADD) $(LIBS)
fish_clusters$(EXEEXT): $(fish_clusters_OBJECTS) $(fish_clusters_DEPENDENCIES) 
	@rm -f fish_clusters$(EXEEXT)
	$(LINK) $(fish_clusters_OBJECTS) $(fish_clusters_LDADD) $(LIBS)
make_template$(EXEEXT): $(make_template_OBJECTS) $(make_template_DEPENDENCIES) 
	@rm -f make_template$(EXEEXT)
	$(LINK) $(make_template_OBJECTS) $(make_template_LDADD) $(LIBS)
masker$(EXEEXT): $(masker_OBJECTS) $(masker_DEPENDENCIES) 
	@rm -f masker$(EXEEXT)
	$(LINK) $(masker_OBJECTS) $(masker_LDADD) $(LIBS)
remove_clusters$(EXEEXT): $(remove_clusters_OBJECTS) $(remove_clusters_DEPENDENCIES) 
	@rm -f remove_clusters$(EXEEXT)
	$(LINK) $(remove_clusters_OBJECTS) $(remove_clusters_LDADD) $(LIBS)
update$(EXEEXT): $(update_OBJECTS) $(update_DEPENDENCIES) 
	@rm -f update$(EXEEXT)
	$(LINK) $(update_OBJECTS) $(update_LDADD) $(LIBS)
update_clusters$(EXEEXT): $(update_clusters_OBJECTS) $(update_clusters_DEPENDENCIES) 
	@rm -f update_clusters$(EXEEXT)
	$(LINK) $(update_clusters_OBJECTS) $(update_clusters_LDADD) $(LIBS)
install-binSCRIPTS: $(bin_SCRIPTS)
	@$(NORMAL_INSTALL)
	test -z "$(bindir)" || $(MKDIR_P) "$(DESTDIR)$(bindir)"
	@list='$(bin_SCRIPTS)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  if test -f $$d$$p; then \
	    f=`echo "$$p" | sed 's|^.*/||;$(transform)'`; \
	    echo " $(binSCRIPT_INSTALL) '$$d$$p' '$(DESTDIR)$(bindir)/$$f'"; \
	    $(binSCRIPT_INSTALL) "$$d$$p" "$(DESTDIR)$(bindir)/$$f"; \
	  else :; fi; \
	done

uninstall-binSCRIPTS:
	@$(NORMAL_UNINSTALL)
	@list='$(bin_SCRIPTS)'; for p in $$list; do \
	  f=`echo "$$p" | sed 's|^.*/||;$(transform)'`; \
	  echo " rm -f '$(DESTDIR)$(bindir)/$$f'"; \
	  rm -f "$(DESTDIR)$(bindir)/$$f"; \
	done

mostlyclean-compile:
	-rm -f *.$(OBJEXT)

distclean-compile:
	-rm -f *.tab.c

include ./$(DEPDIR)/assemble.Po
include ./$(DEPDIR)/assemble1.Po
include ./$(DEPDIR)/assemble_illumina.Po
include ./$(DEPDIR)/build_sff_index.Po
include ./$(DEPDIR)/cluster.Po
include ./$(DEPDIR)/common.Po
include ./$(DEPDIR)/contig.Po
include ./$(DEPDIR)/cull_components.Po
include ./$(DEPDIR)/filter.Po
include ./$(DEPDIR)/filter_clusters.Po
include ./$(DEPDIR)/fish_clusters.Po
include ./$(DEPDIR)/graph.Po
include ./$(DEPDIR)/hashtable.Po
include ./$(DEPDIR)/maf.Po
include ./$(DEPDIR)/make_template.Po
include ./$(DEPDIR)/masker.Po
include ./$(DEPDIR)/mfblocks.Po
include ./$(DEPDIR)/remove_clusters.Po
include ./$(DEPDIR)/sequences.Po
include ./$(DEPDIR)/sff.Po
include ./$(DEPDIR)/sff_index.Po
include ./$(DEPDIR)/slinklist.Po
include ./$(DEPDIR)/splayhash.Po
include ./$(DEPDIR)/update.Po
include ./$(DEPDIR)/update_clusters.Po
include ./$(DEPDIR)/utilities.Po

.c.o:
	$(COMPILE) -MT $@ -MD -MP -MF $(DEPDIR)/$*.Tpo -c -o $@ $<
	mv -f $(DEPDIR)/$*.Tpo $(DEPDIR)/$*.Po
#	source='$<' object='$@' libtool=no \
#	DEPDIR=$(DEPDIR) $(CCDEPMODE) $(depcomp) \
#	$(COMPILE) -c $<

.c.obj:
	$(COMPILE) -MT $@ -MD -MP -MF $(DEPDIR)/$*.Tpo -c -o $@ `$(CYGPATH_W) '$<'`
	mv -f $(DEPDIR)/$*.Tpo $(DEPDIR)/$*.Po
#	source='$<' object='$@' libtool=no \
#	DEPDIR=$(DEPDIR) $(CCDEPMODE) $(depcomp) \
#	$(COMPILE) -c `$(CYGPATH_W) '$<'`

ID: $(HEADERS) $(SOURCES) $(LISP) $(TAGS_FILES)
	list='$(SOURCES) $(HEADERS) $(LISP) $(TAGS_FILES)'; \
	unique=`for i in $$list; do \
	    if test -f "$$i"; then echo $$i; else echo $(srcdir)/$$i; fi; \
	  done | \
	  $(AWK) '{ files[$$0] = 1; nonempty = 1; } \
	      END { if (nonempty) { for (i in files) print i; }; }'`; \
	mkid -fID $$unique
tags: TAGS

TAGS:  $(HEADERS) $(SOURCES) config.h.in $(TAGS_DEPENDENCIES) \
		$(TAGS_FILES) $(LISP)
	tags=; \
	here=`pwd`; \
	list='$(SOURCES) $(HEADERS) config.h.in $(LISP) $(TAGS_FILES)'; \
	unique=`for i in $$list; do \
	    if test -f "$$i"; then echo $$i; else echo $(srcdir)/$$i; fi; \
	  done | \
	  $(AWK) '{ files[$$0] = 1; nonempty = 1; } \
	      END { if (nonempty) { for (i in files) print i; }; }'`; \
	if test -z "$(ETAGS_ARGS)$$tags$$unique"; then :; else \
	  test -n "$$unique" || unique=$$empty_fix; \
	  $(ETAGS) $(ETAGSFLAGS) $(AM_ETAGSFLAGS) $(ETAGS_ARGS) \
	    $$tags $$unique; \
	fi
ctags: CTAGS
CTAGS:  $(HEADERS) $(SOURCES) config.h.in $(TAGS_DEPENDENCIES) \
		$(TAGS_FILES) $(LISP)
	tags=; \
	list='$(SOURCES) $(HEADERS) config.h.in $(LISP) $(TAGS_FILES)'; \
	unique=`for i in $$list; do \
	    if test -f "$$i"; then echo $$i; else echo $(srcdir)/$$i; fi; \
	  done | \
	  $(AWK) '{ files[$$0] = 1; nonempty = 1; } \
	      END { if (nonempty) { for (i in files) print i; }; }'`; \
	test -z "$(CTAGS_ARGS)$$tags$$unique" \
	  || $(CTAGS) $(CTAGSFLAGS) $(AM_CTAGSFLAGS) $(CTAGS_ARGS) \
	     $$tags $$unique

GTAGS:
	here=`$(am__cd) $(top_builddir) && pwd` \
	  && cd $(top_srcdir) \
	  && gtags -i $(GTAGS_ARGS) $$here

distclean-tags:
	-rm -f TAGS ID GTAGS GRTAGS GSYMS GPATH tags

distdir: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	list='$(DISTFILES)'; \
	  dist_files=`for file in $$list; do echo $$file; done | \
	  sed -e "s|^$$srcdirstrip/||;t" \
	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
	case $$dist_files in \
	  */*) $(MKDIR_P) `echo "$$dist_files" | \
			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
			   sort -u` ;; \
	esac; \
	for file in $$dist_files; do \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  if test -d $$d/$$file; then \
	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -pR $(srcdir)/$$file $(distdir)$$dir || exit 1; \
	    fi; \
	    cp -pR $$d/$$file $(distdir)$$dir || exit 1; \
	  else \
	    test -f $(distdir)/$$file \
	    || cp -p $$d/$$file $(distdir)/$$file \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
all-am: Makefile $(PROGRAMS) $(SCRIPTS) config.h
installdirs:
	for dir in "$(DESTDIR)$(bindir)" "$(DESTDIR)$(bindir)"; do \
	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
	done
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	$(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	  install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	  `test -z '$(STRIP)' || \
	    echo "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'"` install
mostlyclean-generic:

clean-generic:

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean: clean-am

clean-am: clean-binPROGRAMS clean-generic mostlyclean-am

distclean: distclean-am
	-rm -rf ./$(DEPDIR)
	-rm -f Makefile
distclean-am: clean-am distclean-compile distclean-generic \
	distclean-hdr distclean-tags

dvi: dvi-am

dvi-am:

html: html-am

info: info-am

info-am:

install-data-am:

install-dvi: install-dvi-am

install-exec-am: install-binPROGRAMS install-binSCRIPTS

install-html: install-html-am

install-info: install-info-am

install-man:

install-pdf: install-pdf-am

install-ps: install-ps-am

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -rf ./$(DEPDIR)
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-compile mostlyclean-generic

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-binPROGRAMS uninstall-binSCRIPTS

.MAKE: install-am install-strip

.PHONY: CTAGS GTAGS all all-am check check-am clean clean-binPROGRAMS \
	clean-generic ctags distclean distclean-compile \
	distclean-generic distclean-hdr distclean-tags distdir dvi \
	dvi-am html html-am info info-am install install-am \
	install-binPROGRAMS install-binSCRIPTS install-data \
	install-data-am install-dvi install-dvi-am install-exec \
	install-exec-am install-html install-html-am install-info \
	install-info-am install-man install-pdf install-pdf-am \
	install-ps install-ps-am install-strip installcheck \
	installcheck-am installdirs maintainer-clean \
	maintainer-clean-generic mostlyclean mostlyclean-compile \
	mostlyclean-generic pdf pdf-am ps ps-am tags uninstall \
	uninstall-am uninstall-binPROGRAMS uninstall-binSCRIPTS


################################################################################
# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
